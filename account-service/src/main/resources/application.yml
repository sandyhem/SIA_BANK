spring:
  datasource:
    url: jdbc:mysql://localhost:3306/account_db
    username: appuser
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8081
  # ssl:
  #   enabled: true
  #   key-store: classpath:ssl/account-service.p12
  #   key-store-password: changeit
  #   key-store-type: PKCS12
  #   trust-store: classpath:ssl/account-truststore.p12
  #   trust-store-password: changeit
  #   trust-store-type: PKCS12
  #   client-auth: need

logging:
  level:
    org.springframework: INFO
    com.banking: DEBUG

feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 5000

# JWT Configuration (must match auth-service secret)
jwt:
  secret: 5367566B59703373367639792F423F4528482B4D6251655468576D5A71347437
